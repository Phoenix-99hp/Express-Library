<h1>{{title}}: {{book.title}}</h1>

{{#if book_instance.length}}
<p><b>Delete the following book instances before attempting to delete this book.</b></p>
<div style='margin-left:20px;margin-top:20px'>

    <h4>Book Instances</h4>

    <dl>
        {{#each book_instance}}
        <dt>
            <a href={{this.url}}> {{../book.title}}</a>
        <dd><b>ID:</b> {{this._id}}</dd>
        {{#ifEquals this.status 'Available'}}
        <dd class="text-success">{{this.status}}</dd>
        {{/ifEquals}}
        {{#ifEquals this.status 'Maintenance'}}
        <dd class="text-danger">{{this.status}}</dd>
        {{/ifEquals}}
        {{#ifEquals this.status 'Loaned'}}
        <dd class="text-warning">{{this.status}}</dd>
        {{/ifEquals}}
        <dd><b>Imprint:</b> {{this.imprint}}</dd>
        <dd><b>Due Back:</b> {{this.due_back_formatted}}</dd>
        </dt>
        {{/each}}
    </dl>
</div>

{{else}}
<p>Do you really want to delete this Book?</p>

<form method='POST' action=''>
    <div class='form-group'>
        <input id='bookid' class='form-control' type='hidden' name='bookid' required='true' value={{book._id}}>
    </div>
    <button class='btn btn-primary' type='submit'>Delete</button>
</form>
{{/if}}